# 4\. 常用数据结构 \&\& 关于单线程模型

# 一\. 常见数据结构 \- 认识数据类型和编码方式

## 1\.1 初步认识数据类型和编码方式

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OGFkZmIwNTExNzU5ZDRiNzBhY2RmY2IzNjYwMWY3NzNfZGVjNDdmYzEyNjBmOTVjNGI2NmMwOGJmYWQ5Nzk1OTFfSUQ6NzY1NzAyNTYzMzkyMzE0MDU2MF8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=YWQzMzdkZDg1ZDRlMDVhOTQ1NDMzNjczYjM2MGEzOWNfZWIyMzc5ZjNiZDE3ZjM1Y2ZiZDNjZDUyMTk0ZjM4NGNfSUQ6NzY1NzAyNjE1OTM2NDEzMTgwMF8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OGYxZjZjMDg2MDUyYTc5YTQxMTg1MjAyZmIzMTE3OGRfMTdjNDc1NWU0N2U5OTY5ZDM0MGNmNTdjNzJlMmU0NmRfSUQ6NzY1NzAyNjIzNDkyMDQyMjYwOV8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

这些数据结构都是我们后续会经常用到的

Redis 底层在实现上述数据结构的时候，会在源码层面，针对上述实现来进行特定的优化，来达到 节省时间/节省空间 效果

内部的具体实现的数据结构（编码方式），可能还会有一些变数



比如：redis 承诺，现在我这有一个 hahs 表，你进行查询，插入，删除操作，都保证时间复杂度 O\(1\)

但是，这个背后的实现，不一定就是一个标准的 hash 表，可能在特定场景下，使用别的数据结构来实现，但是仍然保证时间复杂度符合承诺 \!\!\!

同理，其他数据结构也是一样的，内部针对不同场景会进行优化

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=YzM2OGFkZGY4YTg2MWYzYzE2MWRkYzUzOGE5Y2Y1NWNfOWJlZjhjOTk5OGFjOGFhMGM4ZDFjMzgxNjI2YTBmZDNfSUQ6NzY1NzAyNzQ3NTAyOTg1NTE3N18xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)



- **数据结构**：redis 承诺给你的，也可以理解成数据类型

- **编码方式**：redis 内部底层的实现

同一个数据类型，背后的编码实现方式是不同的，会根据特定场景优化



## 1\.2 数据类型对应实现的编码方式



**string：**

1. raw：最基本的字符串，底层持有一个char数组\(c\+\+\)，或者byte数组\(Java\)

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=MmJlNDczODYyMTk0ZDk5Mzc2ZDE2MTIyMjcxMWE5ZjdfNDkyMmQ0MmU4YjE0MThjNGYyMTkxMDA0YWYzMzhkY2JfSUQ6NzY1NzAyODY3MTQ1NDQ5ODAxNl8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

2. int：redis 通常也可以用来实现一些 “计数” 这样的功能，当 value 就是一个整数的时候，此时 redis 会直接使用 int 来保存\~

3. embstr：针对短字符串进行的优化

Redis 会自动适应，我们一般是感受不到的

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=NjE2OGZhYmM5ZTkwZGJiYzBlYjgwNzA3MmYxZDJjZThfY2FlNGJmNDc1OTE2NGFmMjBhYmEwMWE1NDZlNWIwZTdfSUQ6NzY1NzAyOTMwMjgxNTA2NzMxNF8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)



**hash：**

1. hashtable：最基本的哈希表，虽然这里实现方式可能不太一样，但是整体思想和之前学过的一样

2. ziplist：压缩列表，hash表里面元素比较少的时候。压缩列表可以节省空间，元素比较少遍历一遍也可以看作为O\(1\)；为什么要压缩？redis 上有很多很多 key，可能某些 key 的 value 是 hash，此时，如果 key 特别多，对应的 hash 也特别多，但是每个 hash 又不大的情况下，就尽量去压缩，压缩之后就可以让整体占用的内存变小了。



**list：**

1. linkedlist：链表，元素个数多的时候用

2. ziplist：压缩列表·，元素个数少的时候用，空间利用率高

**从 redis 3\.2 开始，引入了新的实现方式 ****quicklist（同时兼顾了 linkedlist 和 ziplist 的优点）**

quicklist 就是一个链表，每个元素又是一个 ziplist

把空间效率都折中的兼顾到

所以 quicklist 就比较类似于 std::deque



**set：**

1. hashtable：

2. intset：集合中存的都是整数



**zset：**

1. skiplist：跳表，不同于普通的链表，每个节点上有多个指针域，巧妙的搭配这些指针域的指向，就可以做到，从跳表上查询元素的时间复杂度 O\(logn\)

2. ziplist：



**object encoding key \-\- 可以查看实际的编码方式**

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=ZWUzYTBkYzRkM2Y0MzMxOWU4MTYzZTE0YzU3MjYzMDFfMzA4OWQ0ODk0NGU5NzQxN2VjN2EyODI4NTFiNmJlNDNfSUQ6NzY1NzA1NzY5MTk2MTEwMTI4N18xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

Redis 会自动根据当前的实际情况选择内部的编码方式，自动适应的

是否需要记住，啥时候使用啥编码方式呢？？

只记思想，不记数字！记数字没有任何意义

1. 数字都是可以配置的

2. 数字是怎么来的，需要考证清楚

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OGM4ZDk1NzIwZWM2OTI5N2E0MGFkMDk4NWYyZDUyNjFfNWY2Y2I0ZDM1Y2JiNGY5ZTE3MjRlNDU0ZGMxZDFjZTJfSUQ6NzY1NzA1ODEzNjM3NzY3NDcwM18xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

相比于知道数字，更重要的是知道数字是怎么得到的，就可以根据所处的实际场景，重新得到这样的数字

很多人会陷入 “记忆数字” 的误区，因为学校的考试就爱考这些问题，但是考试和面试，和工作，都差别很大

- **考试的评价标准**：有标准答案，看你做的对不对

- **面试的评价标准**：有部分标准答案，也有一部分没有标准答案，主要是看你解决问题的过程和思路 \-\> 面试官是否认可你这个人

- **实际工作的评价标准**：低成本的解决问题

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=MDI0MGZkYTJmODgwYjJhMTA5ZjVkNmM4MTM2MjMyMzVfYjBjNzY5ODY4NTFjNGRhNjc2OTFiNDRkNGEyNDczYzdfSUQ6NzY1NzA1ODkzOTgzNTM4NzA5MV8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OGFhZGI2YTMxNGU3MmZkMWRmNGZmNGJhMjJlZjExMDhfZmM4ODUwNDBlOTQxNTcyMWMyODEzZDk0MzNmNTBhOGVfSUQ6NzY1NzA1OTQxOTA3NDk3MjkwNF8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

类似于这样的一些 “参数” 是非常常见的，都是 “可调” 的

# 二\. 关于单线程模型

## 2\.1 单线程模型的工作过程

**Redis 单线程模型，redis 只使用一个线程，处理所有的命令请求，不是说一个 redis 服务器进程内部真的就只有一个线程，其实也有多个线程，多个线程是处理 网络 IO**



假设，有多个客户端，同时操作一个 redis 服务器

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=ZDNhODI0NDI1MzZlNWViNmFmMjJiOGIzY2ZmM2M1MjdfMWE1NTkwOGMyMGIwOGExYzhlNTMwMTA2Zjc5MWE5MzNfSUQ6NzY1NzA2MDM4NDQ4NjcyMjUxN18xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

我们之前多线程的时候讨论过线程安全问题

在多线程中，针对类似的场景，两个线程尝试同时对一个变量进行自增，表面上看是自增两次，实际上可能只自增了一次（并发引起的问题）



当前这两个客户端，也相当于 “并发” 的发起了上述的请求，此时就意味着是否服务器这边也会存在类似的线程安全问题呢？

幸运的是，并不会，redis 服务器实际上是单线程模型，保证了当前收到的这多个请求是串行执行的

多个请求同时到达 redis 服务器，也是要先在队列中排队，再等待 redis 服务器一个个的取出里面的命令再执行，微观上来讲，redis 服务器是串行/顺序执行这多个命令的

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=MzRiYjUzNDRiNWZiZWMwY2IyNmU4N2IxYTc0YTRmYTVfOTMxNzYxMTI4ZDg4ZGQxMTBhNjQ1MGY0NGYzMWQyMTdfSUQ6NzY1NzA2MTU5MzA5NjM1OTEwMl8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=NGNkZTYxZjBiZmUzMTdlNzA4MjVhMDk5YTU5MWZiMDJfMjI0MTRhNjgwODBhYTQ0OGNlOTE5MDQyZmNhNGM4ZTFfSUQ6NzY1NzA2MTY5MzgxODE0NTcyOF8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

Redis 能够使用 单线程模型 很好的工作，原因主要在于 redis 的核心业务逻辑，都是短平快的\~，不太消耗 CPU 资源也就不太吃多核了，但是要是你的业务是CPU密集型的，要消耗CPU资源你肯定就不能单线程了

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=ZDQ5NzEwYTMwMThlNTQwNTZiNGE2Yjc0MWRiNzY2Y2JfNWI1OWFhNzc1OTUxMzFkNTI4YWY5ZGMxYjkwMzdkYTlfSUQ6NzY1NzA2MjExNzA5MDU2MTI3MV8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

**用单线程当然也是有一些弊端的**：redis 必须要特别小心，某个操作占用时间长，就会阻塞其他命令的执行（比如我们之前说的 keys \* 的问题）

## 2\.2 单线程为什么快

**Redis 虽然是单线程模型，但是为啥效率这么高呢？速速度这么快呢？【重要的面试题】**

我们说的效率高，速度快都是和数据库（比如MYSQL）这种关系型的数据库进行比较的

1. Redis 的操作主要就是访问内存，数据库则是访问硬盘

2. Redis 核心功能，比数据库的核心功能更简单，这个点是很重要的 

数据库对于数据的插入删除查询\.\.\.\.，都有更复杂的功能支持，这样的功能势必要花费更多的开销，比如，针对插入删除，数据库中的各种约束，都会使数据库做额外的工作。比如你主键约束了，你插入之前还得先查一下，还有MySQL的查询也是有很多种形式的，可以分组啊，限定条件啊等等

Redis 就很简单了，根据 key 查 value

Redis 干的活少，提供的功能相比于 MySQL 也是少了不少

3. 单线程模型，避免了一些不必要的线程竞争开销

Redis 每个基本操作，都是短平快的，就是简单操作，就是简单操作了一下内存数据，不是特别消耗 cpu 的操作，就算搞个多个线程，也是提升不大的

4. 处理网络 IO 的时候，使用了 epoll 这样的 IO 多路复用机制

一个线程就可以管理多个 socket，针对 TCP 来说，服务器每次服务一个客户端都需要安排一个socket，和客户端进行通信

现在我们假设一个服务器服务多个客户端，同时有很多个socket，这些socket上都是无时不刻的在传输数据嘛？肯定不是的，其实更多的时间是在等待

很多情况下，每个客户端和服务器之间的通信也没那么频繁（当然那些下载软件，看直播可能频繁点）

此时这么多 socket 大部分时间都是 静默的，上面是没有数据需要传输的

同一时刻，只有少数 socket 是活跃的，最开始介绍 TCP 服务器的时候，有一个版本就是每个客户端给分配一个线程，这个操作肯定是可行的，但是客户端多了，线程就多了，系统开销就大了

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=ZmQ5YWRkYTY1OTcxNDA5NWE5YzRhZTMxN2I3OTBhZGRfNmRhNGM5M2NkY2MyNWQ5MWE0NTI3MDY2YjJiZTZiOWRfSUQ6NzY1NzA2NTQxNTI0ODQ5Nzg3N18xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

基于上述的这些原因，我们就搞了 IO 多路复用，一个线程处理多个 socket，这个是操作系统给我们提供的机制，提供了一套API，内部的功能都是操作系统内核实现的

Linux上提供的 IO 多路复用，主要是三套 API

1. select

2. poll

3. epoll（2006年左右，运行效率最高的机制）

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OTQyZDM2ZTkxYmU1OGIxNmNkY2RjZDUyMDZiNWU2YzhfZmE3YmViYzQwNzM5NDgxZDUxNjE5NDQxNWIyNGE4YmFfSUQ6NzY1NzA2NjU1OTg5MDA2NjM4M18xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=ZTEyMzNmNzRhNzc4Y2ExMDU5MmMzOGUxOWFmOTNmMWFfZjdmOThiNDM0NjViMzRkZWE3OGFhMDM5MWNhOTNjYTVfSUQ6NzY1NzA2ODQ0NDE4NTU0NTY5Ml8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)

充分利用了等待的时间，epoll 就是一个事件通知，回调机制

select 就比较麻烦了，好了没人告诉我，我得遍历去检查

如果这三件事都是交互特别频繁的，还是老老实实多搞几个线程靠谱，一个线程就容易忙不过来

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=Y2ZiNDhkZTJmYjkwOWFhMDlmOWFkODQyMWQwNDU3NmVfZmEyZmZiNDNkNzU4YWUxOGJmNTFlMWQ5MTEyN2NhOTJfSUQ6NzY1NzA2OTI5NTkwMTc3Mjk5OF8xNzgyODAwOTI4OjE3ODI4ODczMjhfVjM)



